#include <iostream>
#include <vector>

#include <Int_Vector.h>

int main()
{
    Int_Vector vector;

    vector.push_back(7);
    vector.push_back(3);
    vector.push_back(6);
    vector.push_back(9);
    vector.push_back(6);
    vector.push_back(9);

    print(vector);
    std::vector<int>
}
